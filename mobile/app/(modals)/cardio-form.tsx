import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import SafeView from '../../components/safe-view'

const CardioForm = () => {
  return (
    <SafeView>
      <Text>CardioForm</Text>
    </SafeView>
  )
}

export default CardioForm

const styles = StyleSheet.create({})
